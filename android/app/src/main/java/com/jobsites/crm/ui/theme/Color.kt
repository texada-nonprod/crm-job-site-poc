package com.jobsites.crm.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand ────────────────────────────────────────────────────────────
val Primary = Color(0xFF0052FF)          // Web: hsl(221, 97%, 54%)
val PrimaryLight = Color(0xFF4D88FF)     // Sidebar-primary dark
val OnPrimary = Color.White

// ── Status badge colors (matches Tailwind classes used in web) ──────
val StatusActive = Color(0xFF10B981)     // Emerald-500
val StatusActiveBg = Color(0xFFD1FAE5)   // Emerald-100
val StatusPlanning = Color(0xFF0EA5E9)   // Sky-500
val StatusPlanningBg = Color(0xFFE0F2FE) // Sky-100
val StatusOnHold = Color(0xFFF59E0B)     // Amber-500
val StatusOnHoldBg = Color(0xFFFEF3C7)   // Amber-100
val StatusCompleted = Color(0xFF64748B)  // Slate-500
val StatusCompletedBg = Color(0xFFF1F5F9)// Slate-100

// ── Semantic ─────────────────────────────────────────────────────────
val Error = Color(0xFFF75A5A)            // Destructive light
val ErrorDark = Color(0xFFC92E2E)        // Destructive dark
val OnError = Color.White

// ── Revenue chip colors ──────────────────────────────────────────────
val RevenueWon = Color(0xFF10B981)       // Emerald-500
val RevenuePipeline = Color(0xFF0EA5E9)  // Sky-500
val RevenueTotal = Color(0xFF6366F1)     // Indigo-500

// ── Surface / Background ─────────────────────────────────────────────
val LightBackground = Color(0xFFFAFAFA)  // Neutral-50
val LightSurface = Color.White
val LightOnBackground = Color(0xFF0A0E27) // Web foreground
val LightOnSurface = Color(0xFF0A0E27)
val LightSurfaceVariant = Color(0xFFF0F6FB) // Secondary
val LightOnSurfaceVariant = Color(0xFF1F2947) // Secondary-foreground
val LightOutline = Color(0xFFE5EAEF)     // Border

val DarkBackground = Color(0xFF0A0E27)
val DarkSurface = Color(0xFF1F2B42)
val DarkOnBackground = Color(0xFFF5FAFE)
val DarkOnSurface = Color(0xFFF5FAFE)
val DarkSurfaceVariant = Color(0xFF1F2B42)
val DarkOnSurfaceVariant = Color(0xFFF5FAFE)
val DarkOutline = Color(0xFF1F2B42)

// ── Note tag colors (matches web) ────────────────────────────────────
val TagRed = Color(0xFFEF4444)
val TagAmber = Color(0xFFF59E0B)
val TagSky = Color(0xFF0EA5E9)
val TagSlate = Color(0xFF64748B)
